import React from "react";

const Header = () => {
    return (
        <header className="CSS_X_LANG_DIRECTION">
            <h1 className="jclc">Prayer Time Application</h1>
        </header>
    );
};

export default Header;